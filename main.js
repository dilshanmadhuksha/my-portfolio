var typed = new Typed(".text", { // Ensure Typed is capitalized
    strings: ["Frontend developer", "Data analyst"], // Use correct strings
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true,
});
